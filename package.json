{
  "name": "examsetu-doc-convert-backend",
  "version": "1.0.0",
  "description": "Converts legacy DOC/DOCX equations (WMF/OLE objects from old MS Equation Editor 3.0) and embedded images into web-friendly PNGs, so ExamSetu's document import can display them correctly.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "jszip": "^3.10.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
